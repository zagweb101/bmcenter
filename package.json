{
  "name": "bayt-almoswer",
  "version": "0.0.0",
  "private": true,
  "description": "منظومة بيت المصور الرقمية المتكاملة — BAYT ALMOSWER ACADEMY",
  "license": "UNLICENSED",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "echo \"(placeholder) configure per-workspace lint in MVP-0A\"",
    "typecheck": "echo \"(placeholder) configure per-workspace typecheck in MVP-0A\"",
    "test": "echo \"(placeholder) configure tests in MVP-0A\""
  }
}
