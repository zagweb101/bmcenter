"use client";

import { AppShell } from "@/components/AppShell";

export default function ConsentsPage() {
  return (
    <AppShell>
      <div className="mx-auto max-w-4xl">
        <h1 className="mb-2 text-xl font-bold text-brand-navy">الموافقات</h1>
        <p className="text-brand-navy/50">
          واجهة الموافقات قيد الإنشاء — الـ API جاهز (PRD §19.4).
        </p>
      </div>
    </AppShell>
  );
}
